---
docker_download_url: "https://download.docker.com"
linux_distro: "raspbian"
dist_version: "{{ ansible_distribution_release }}"
docker_key_url: "{{ docker_download_url }}/linux/{{ linux_distro }}/gpg"
channel: "stable"

